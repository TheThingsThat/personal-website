import { Clip } from "./clip";
import { Figure } from "./figure";
import { YouTube } from "./youtube";

/** Components available inside every MDX file, no imports needed. */
export const mdxComponents = {
  Figure,
  Clip,
  YouTube,
};
