/** Site-wide constants. Change these once, everything (metadata, RSS,
 *  sitemap, Open Graph) follows. `url` must match the primary serving domain
 *  — absolute URLs in the RSS feed, sitemap, and canonicals come from it. */
export const site = {
  name: "Tim Tian",
  url: "https://www.timtian.dev",
  description: "Essays, code, builds, notes, and interests.",
};
