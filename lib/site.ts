/** Site-wide constants. Change these once, everything (metadata, RSS,
 *  sitemap, Open Graph) follows. Set `url` to your real domain before
 *  deploying — absolute URLs in the RSS feed and sitemap come from it. */
export const site = {
  name: "Tim Tian",
  url: "https://example.com",
  description: "Essays, code, builds, notes, and interests.",
};
